import {{MODULE_EVENT}} from "../event/{{MODULE_EVENT}}";
{{#if RUNTIME_WEB}}
import {FabaWebCommand} from "@fabalous/runtime-web/FabaWebCommand";
import WebStore from "../../AppState";

export default class {{MODULE_COMMAND}} extends FabaWebCommand<AppState> {
{{/if}}
    execute(event:{{MODULE_EVENT}}) {
    }
}