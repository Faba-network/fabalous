import * as React from "react";
import AppState from "../../AppState";

import {connectWithRoute} from "@fabalous/runtime-web/utils/connect";
import {IRoutes} from "@fabalous/runtime-web/routes/IRoutes";

interface Props{
}

class {{UPPER_MODULE_NAME}} extends React.PureComponent<Props>{
    render(): React.ReactElement<Props> {
        return(
            <>
                {{UPPER_MODULE_NAME}}
            </>
        )
    }
}

function mapStateToProps (state: AppState, route:IRoutes): Props {
    return {
    }
};

export default connectWithRoute(mapStateToProps)({{UPPER_MODULE_NAME}});
